# Credit for base environment file from course material of DSCI532

name: 'vanweather'
channels:
    - conda-forge
    - defaults
    - pip
dependencies:
    - ipykernel
    - altair-all
    - dash
    - dash-bootstrap-components
    - plotly
    - r-base
    - r-tidyverse
    - r-shiny
    - r-languageserver
    - r-plotly
    - pip
    - pip:
        - dash-vega-components
        - openmeteo_requests
        - requests_cache
        - retry_requests
        - jupyter
